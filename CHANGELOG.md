# Remake Changelog

## Pre-Releases:

### v0.3.0
 - `rm`, `open`, and `update` commands added.
 - `ls` changes.

### v0.2.0
 - `ls` command. 
 - Major refactor/restructure.  


### v0.1.0
`add source`, `copy`, and `clip` commands.